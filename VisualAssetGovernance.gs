/**
 * Visual Asset Metadata Governance Validator
 *
 * The implementation is split across VAM_*.gs modules for faster review, testing,
 * and safer future patches. Public entry points are preserved in VAM_Public.gs.
 */
